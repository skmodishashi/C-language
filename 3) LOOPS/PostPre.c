#include <stdio.h>
int main()
{
    int x = 5;
    printf("%d\n", x);
    x++; // x = x + 1 //x++ means use x , then increment  OR ++x means, increment then use 
    printf("%d\n", x);
    return 0;
}